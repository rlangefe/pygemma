import numpy as np

cdef likelihood(double lamb,
                double tau,
                np.ndarray[double, ndim=1] alpha,
                double beta,
                np.ndarray[double, ndim=2] W,
                np.ndarray[double, ndim=1] x):
    

cdef likelihood_restricted():